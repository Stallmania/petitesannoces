{% extends 'users.html.twig' %}

{% block title %}Modification du profil Utilisateur
{% endblock %}

{% block body %}
	<div class="grix xs4 container">
		<div class="col-xs4">
			<h1>edition du Profile de
				{{ app.user.name }}
				{{ app.user.firstname }}
			</h1>
            {{ form(form)}}
		</div>
	</div>
{% endblock %}
